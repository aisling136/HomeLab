---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qui
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: media
  resources:
    requests:
      storage: 1Gi
