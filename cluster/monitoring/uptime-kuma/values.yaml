app-template:
  controllers:
    uptime-kuma:
      annotations:
        reloader.stakater.com/auto: "true"
      labels:
        app: &app uptime-kuma
      containers:
        uptime-kuma:
          env:
            TZ: America/New_York
          image:
            repository: louislam/uptime-kuma
            tag: 2.0.1@sha256:60e6fe0c481b452633cef1a47c1096633b36e6871c5cd97ec61039de1706e1f3
          ports:
            - name: http
              containerPort: 3001
              protocol: TCP
          resources:
            requests:
              cpu: 10m
              memory: 50Mi
            limits:
              memory: 150Mi
  service:
    uptime-kuma:
      controller: *app
      labels:
        app: *app
      ports:
        http:
          port: 3001
          protocol: TCP
  ingress:
    uptime-kuma:
      className: external
      annotations:
        gethomepage.dev/enabled: "true"
        gethomepage.dev/icon: uptime-kuma.svg
        gethomepage.dev/name: "Uptime Kuma"
        gethomepage.dev/group: Monitoring
        external-dns.alpha.kubernetes.io/target: "external.dex136.xyz"
      hosts:
        - host: kuma.dex136.xyz
          paths:
            - path: /
              service:
                identifier: *app
                port: http
  persistence:
    data:
      existingClaim: pvc-uptime-kuma
      globalMounts:
        - path: /app/data
