---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minecraft
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: media
  resources:
    requests:
      storage: 30Gi
