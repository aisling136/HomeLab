apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  annotations:
    external-dns.alpha.kubernetes.io/target: gw-external.dex136.xyz
  name: argocd-server-wh
  namespace: argocd
spec:
  parentRefs:
    - name: envoy-external
      namespace: network
  hostnames:
    - awh.dex136.xyz
  rules:
  - backendRefs:
    - group: ""
      kind: Service
      name: argocd-server
      namespace: default
      port: 80
      weight: 1
    matches:
    - path:
        type: PathPrefix
        value: /api/webhook

